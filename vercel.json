{
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "devCommand": "npm run server",
  "installCommand": "npm ci",
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*"
    }
  ]
}
